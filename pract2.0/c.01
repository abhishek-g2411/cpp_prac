#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int x, y;
        cin >> x >> y;
        // Compare the cost of 3 double rooms vs 2 triple rooms
        cout << min(3 * x, 2 * y) << endl;
    }
    return 0;
}